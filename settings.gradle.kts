pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "ciudademojis"
enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")

include(":composeApp")